<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ToyProject.user.mapper.UserMapper">
<!--
  <insert id="insertUser" parameterType="com.Toyproject.user.vo.UserVo">
  INSERT INTO USERS (USER_NO,USER_ID,PASSWORD,NAME,EMAIL,PHONE,ZCODE,ADDR1,ADDR2,CRT_DT,UPD_DT,USE_YN,AUTH_CODE,POINT,GRADE)
  VALUES
  (#{USER_NO},#{USER_ID},#{PASSWORD},#{NAME},#{EMAIL},#{PHONE},#{ZCODE},#{ADDR1},#{ADDR2},#{CRT_DT},#{UPD_DT},#{USE_YN},#{AUTH_CODE},#{POINT},#{GRADE})
  </INSERT>
-->

  <insert id="insertUser" parameterType="com.ToyProject.user.vo.TestVo">
 INSERT INTO USERS 
  (USER_NO, USERNAME, PASSWORD, ROLE)
  VALUES
  (#{USER_NO},#{username},#{password},'ROLE_USER')
  </insert>




  <select id="selectUser" resultType="com.ToyProject.user.vo.TestVo">
    SELECT * FROM users
    WHERE (USER_ID=#{userID});
  </select>
  
</mapper>
